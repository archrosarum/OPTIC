#pragma once

#include "../shared.h"

namespace OPTIC {
    class Node {
    public:
        Node(std::string identifier);
        ~Node();

        std::string identifier;
        
    private:

    };
}
